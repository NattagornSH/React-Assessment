# my-understanding

ผมทำโปรเจคนี้โดยแบ่ง component ตามหน้าที่การใช้งาน มี Navbar สำหรับเมนู Home กับ Owner เป็นหน้าหลัก แล้วก็มี UserSection กับ AdminSection ที่แยกกันเพราะทำงานต่างกัน อันนึงดูอย่างเดียว อีกอันแก้ไขได้ ส่วน MembersTable ก็ทำเป็น component แยกเพื่อใช้ซ้ำได้ทั้งสองที่

เรื่อง state ผมใช้ activeSection เก็บว่าเปิด tab ไหนอยู่ members เก็บข้อมูลจาก API loading เช็คว่าโหลดเสร็จยัง formData เก็บค่าที่กรอกในฟอร์ม ตอนแรกก็งงว่าอะไรควรเป็น state บ้าง แต่พอทำไปก็เข้าใจว่าถ้าข้อมูลเปลี่ยนแล้วหน้าจอต้องอัพเดทก็ต้องใช้ state

การจัดการ state ผมใช้แบบส่ง props ธรรมดา ไม่ได้ใช้ Context เพราะโปรเจคไม่ใหญ่มาก ก็แค่ส่งจาก Home ไป UserSection/AdminSection แล้วส่งต่อไป MembersTable ตอนแรกก็เคยได้ยินเรื่อง Context แต่ยังไม่รู้ว่าใช้ยังไง

useEffect ใช้สำหรับเรียก API ตอน component โหลดครั้งแรก ถ้าไม่ใช้มันจะวนลูปไม่หยุดเพราะทุกครั้งที่ setState มันก็ render ใหม่แล้วก็ fetch อีก พอใส่ dependency array เป็น [] มันก็ fetch แค่ครั้งเดียว ตอนแรกไม่เข้าใจทำไมต้องใช้ แต่พอเจอ bug แล้วก็เข้าใจ

fetch ต้องใช้กับ useEffect ถ้าจะดึงข้อมูลตอนเริ่มต้น แต่ถ้าเป็นตอนกดปุ่มก็ไม่ต้อง เพราะมันเกิดจาก event ไม่ใช่ตอน render แล้วก็ fetch ต้องเป็น async เสมอเพราะต้องรอข้อมูลจาก server ถ้าเป็น sync หน้าจอจะค้าง ก็เลยต้องใช้ async/await

ที่เหลือก็ยังมีอีกเยอะที่ต้องเรียนรู้ เช่น error handling ตอนนี้ก็แค่ console.error form validation ก็แค่ required ถ้าข้อมูลเยอะควรใส่ pagination ไหม แล้วก็ยังไม่รู้ว่าถ้าโปรเจคใหญ่ขึ้นจะจัดการ state ยังไง ควรใช้ Redux หรือเปล่า

โดยรวมแล้วได้ลองใช้ React Router เรียก API ทำ CRUD ใช้ Tailwind รู้สึกว่าเข้าใจ React มากขึ้น แต่ก็ยังมีอีกเยอะที่ต้องเรียน

---

AI reliance: 4/5
